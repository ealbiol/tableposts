import { render } from '@testing-library/react';
import SidebarLayout from './SidebarLayout';

describe("Test 1 - Basic render:", () => {
    test("Check if SidebarLayout component renders without problems", () => {
        render(<SidebarLayout />)

    })
})